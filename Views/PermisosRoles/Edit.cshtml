@model Oasis.Models.PermisosRole

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>PermisosRole</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CodigoPermisoRol" />
            <div class="form-group">
                <label asp-for="CodigoRol" class="control-label"></label>
                <select asp-for="CodigoRol" class="form-control" asp-items="ViewBag.CodigoRol"></select>
                <span asp-validation-for="CodigoRol" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CodigoPermiso" class="control-label"></label>
                <select asp-for="CodigoPermiso" class="form-control" asp-items="ViewBag.CodigoPermiso"></select>
                <span asp-validation-for="CodigoPermiso" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
